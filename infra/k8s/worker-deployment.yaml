# BeGuardit Worker — Deployment (§14.3)
# No Service needed: the worker pulls jobs from Redis, it does not accept inbound traffic.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: beguardit-worker
  namespace: beguardit
  labels:
    app.kubernetes.io/name: beguardit-worker
    app.kubernetes.io/component: worker
    app.kubernetes.io/part-of: beguardit
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: beguardit-worker
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: beguardit-worker
        app.kubernetes.io/component: worker
        app.kubernetes.io/part-of: beguardit
    spec:
      containers:
        - name: worker
          image: ghcr.io/beguardit/worker:latest
          command: ["arq", "src.main.WorkerSettings"]
          envFrom:
            - secretRef:
                name: beguardit-secrets
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
          volumeMounts:
            - name: report-data
              mountPath: /data/reports
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
      volumes:
        - name: report-data
          persistentVolumeClaim:
            claimName: beguardit-reports
      restartPolicy: Always
