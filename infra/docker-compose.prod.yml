# Production compose — used alongside docker-compose.yml via -f flag
# Overrides for production: no --reload, no dev ports, image tags pinned
services:
  api:
    command: uvicorn src.main:create_app --factory --host 0.0.0.0 --port 8000 --workers 2

  portal:
    command: nginx -g "daemon off;"
